# frontend-coding-standard

NgModule, `standalone: false`, constructor DI, signals for UI state, shared layer for controls, translate all visible text.

## Required Flow

1. Read docs first.
2. Inspect existing pattern.
3. Implement narrow change.
4. Update docs if contract/folder/UI behavior changes.
5. Run 
pm.cmd run build and relevant tests.

