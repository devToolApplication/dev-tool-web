# add-new-shared-ui

Only domain-free reusable UI block goes to `shared/ui`; document inputs/outputs and states.

## Required Flow

1. Read docs first.
2. Inspect existing pattern.
3. Implement narrow change.
4. Update docs if contract/folder/UI behavior changes.
5. Run 
pm.cmd run build and relevant tests.

