# add-new-store

Store exposes signals/computed for loading/error/selected/filter/data; handles API mapping and refresh.

## Required Flow

1. Read docs first.
2. Inspect existing pattern.
3. Implement narrow change.
4. Update docs if contract/folder/UI behavior changes.
5. Run 
pm.cmd run build and relevant tests.

