# Grill Me

## Decisions Already Answered By User

- FE first.
- Pure Flowable Service Task configuration.
- No topic-specific presets.
- No legacy `taskConfigJson` editor.
- Input and output mapping should feel like Camunda.
- Backend should add Flowable libraries and JavaDelegate classes, not custom FE
  contracts.

## Skill Preflight

- Exact `grill-me` skill is not installed.
- `grilling` exists, but the user already supplied the necessary decisions for
  this implementation round.

